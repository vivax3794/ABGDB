# the file where the database will be stored.
DATABASE = "Database.db"

# the cogs to load, best to just have all of there
COGS = [
        "settings",
        "admin",
        "help"
        ]

# this is not admins on your server, but admins of the bot
# only give this to trusted users, they can run code on the bot and can leek your token!
# the owner of the bot application will automaticly be considerd a admin.
ADMINS = [
        366331361583169537  # vivax, the creator of the bot.
        ]
